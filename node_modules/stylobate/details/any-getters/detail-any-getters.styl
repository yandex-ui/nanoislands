any_get_sides($where, $fallback = 0)
  $this_sides = utilus_get_sides($where, $fallback)
  if ('' + $this_sides == '0 0 0 0')
    return utilus_get_sides(utilus_get($where, parent), $fallback)
  else
    return $this_sides
