utilus_contrast($top, $bottom = #FFF)
  warn('`utilus_contrast` is deprecated, use `contrast` from Stylus itself.')
  $contrast = contrast($top, $bottom)

  return $contrast.ratio $contrast.min $contrast.max
