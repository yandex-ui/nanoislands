utilus_rgb2rgba($foreground, $background = #FFF)
  // warn('`utilus_luminance` is deprecated, use `luminosity` from Stylus itself.')
  return transparentify($foreground, $background)

rgb2rgba ?= utilus_rgb2rgba
