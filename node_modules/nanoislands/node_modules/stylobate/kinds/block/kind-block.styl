kind-block()
  position: utilus_retrieve(arguments, static, relative)

  if inline in arguments
    display: inline-block
    vertical-align: utilus_retrieve(arguments, middle baseline bottom, top)
  else
    display: block
