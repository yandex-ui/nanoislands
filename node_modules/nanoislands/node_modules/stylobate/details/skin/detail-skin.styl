$skin_namespace ?= ''

set-skin-namespace($what)
  $what = $what + '-' if $what != ''
  utilus_reassign($skin_namespace, $what)

reset-skin-namespace()
  utilus_reassign($skin_namespace, '')

skin()
  stylobate('skin-' + $skin_namespace, arguments)

global_skin()
  stylobate('skin-', arguments)
