kind-hidden($this)
  $type = $this.idents[0]

  if $type == 'visually'
    +cache()
      position: absolute !important
      clip: rect(1px,1px,1px,1px) !important
  else
    display: none !important
