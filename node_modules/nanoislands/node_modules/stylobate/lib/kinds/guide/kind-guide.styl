kind-guide($this)
  if $this.params.visible
    pointer-events: none
  else
    visibility: hidden

stylobate_extends('kind-guide', 'kind-fill')

kind-guide__item($this)
  if $this.params.visible
    pointer-events: auto
  else
    visibility: visible
