kind-guide()
  if visible in arguments
    pointer-events: none
  else
    visibility: hidden

stylobate_extends('kind-guide', 'kind-fill')

kind-guide-item()
  if visible in arguments or visible in utilus_get(arguments, parent)
    pointer-events: auto
  else
    visibility: visible
