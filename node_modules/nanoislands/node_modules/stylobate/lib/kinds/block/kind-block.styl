kind-block($this)
  position: utilus_retrieve($this.params, static, relative)

  if $this.params.inline
    display: inline-block
    vertical-align: utilus_retrieve($this.params, middle baseline bottom, top)
  else
    display: unquote('block')
