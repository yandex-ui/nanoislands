kind-block($this)
  position: relative

  if $this.params.inline
    display: inline-block
    vertical-align: top
  else
    display: unquote('block')

  deprecate($this.params, 'middle')
  deprecate($this.params, 'baseline')
  deprecate($this.params, 'bottom')
