skin-islands-group()
  kind: group

$skin-islands-group
  &:before
    right: 0

  input&,
  label&
    border-right-color: transparent

$skin-islands-group_vertical
  &:before
    bottom: 0

  input&,
  label&
    border-bottom-color: transparent

skin-islands-group__start($this)
  kind: group__start '&,&:before' $this.params.vertical

  $placeholder = $this.params.vertical ? '$skin-islands-group_vertical' : '$skin-islands-group'
  @extends {$placeholder}

skin-islands-group__middle($this)
  kind: group__middle '&,&:before' $this.params.vertical

  $placeholder = $this.params.vertical ? '$skin-islands-group_vertical' : '$skin-islands-group'
  @extends {$placeholder}

skin-islands-group__end($this)
  kind: group__end '&,&:before' $this.params.vertical

skin-islands-group__item($this)
  &:first-child:not(:only-child)
    skin-islands-group__start($this)

  &:not(:first-child):not(:last-child)
    skin-islands-group__middle($this)

  &:last-child:not(:only-child)
    skin-islands-group__end($this)

skin-islands-nested-group__item($this)
  $nested-group-item = $this.selectors[0] or '.toggler-view'
  kind: group__item ('& > ' + $nested-group-item + ', & > ' + $nested-group-item + ':before') $this.params.vertical

  &:not(:last-child):not(:only-child) > {$nested-group-item}
    $placeholder = $this.params.vertical ? '$skin-islands-group_vertical' : '$skin-islands-group'
    @extends {$placeholder}

