func loaders() {

    show('loader-s', nb-loader({
        'size': 'xs'
    }))

    show('loader-s', nb-loader({
        'size': 's'
    }))

    show('loader-m', nb-loader())

    show('loader-s', nb-loader({
        'size': 'l'
    }))
}