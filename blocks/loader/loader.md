### Default loader
> <div example="loader-m"/>
>
> ```yate
>     nb-loader()
> ```

* `size`: `m`

### Options

* `size` {string} — spinner size — xs / s / m / l

### Examples

Small loader:
> <div example="loader-s"/>
>
> ```yate
>     nb-loader({
>         'size': 's'
>     })
> ```
